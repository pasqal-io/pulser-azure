{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Pulser Azure","text":"<p>Pulser Azure is a Python package to run quantum sequences on Azure Quantum infrastructure, providing access to Pasqal neutral atom quantum computers.</p> <p>It implements the Pulser <code>RemoteConnection</code> interface so any sequence written with Pulser can be submitted to Pasqal QPUs and emulators hosted on Azure Quantum.</p>"},{"location":"#installation","title":"Installation","text":"<pre><code>pip install pulser-azure\n</code></pre>"},{"location":"#at-a-glance","title":"At a glance","text":"<pre><code>from pulser_azure import AzureConnection\n\nconnection = AzureConnection(\n    resource_id=\"/subscriptions/&lt;id&gt;/resourceGroups/&lt;rg&gt;/providers/Microsoft.Quantum/Workspaces/&lt;ws&gt;\",\n)\n\ndevices = connection.fetch_available_devices()\n</code></pre>"},{"location":"#where-to-next","title":"Where to next?","text":"<ul> <li>Getting started \u2014 full end-to-end example</li> <li>API reference \u2014 auto-generated from the source</li> </ul>"},{"location":"#license","title":"License","text":"<p>Apache 2.0 \u2014 see LICENSE.</p>"},{"location":"getting-started/","title":"Getting started","text":""},{"location":"getting-started/#authenticating","title":"Authenticating","text":"<p>To instantiate the <code>AzureConnection</code>, you need to provide a <code>resource_id</code>:</p> <pre><code>from pulser_azure import AzureConnection\n\nconnection = AzureConnection(\n    resource_id=\"/subscriptions/&lt;your-subscription-id&gt;/resourceGroups/&lt;your-resource-group-name&gt;/providers/Microsoft.Quantum/Workspaces/&lt;your-workspace-name&gt;\",\n)\n</code></pre> <p>Alternatively, the <code>AzureConnection</code> can discover your resource from environment variables:</p> <pre><code>export PULSER_AZURE_RESOURCE_ID=\"/subscriptions/&lt;id&gt;/resourceGroups/&lt;rg&gt;/providers/Microsoft.Quantum/Workspaces/&lt;ws&gt;\"\n</code></pre> <p>Then you can instantiate the provider without any arguments:</p> <pre><code>from pulser_azure import AzureConnection\n\nconnection = AzureConnection()\n</code></pre>"},{"location":"getting-started/#running-a-sequence-on-a-qpu","title":"Running a sequence on a QPU","text":"<p>See the Pulser sequence documentation for the full sequence-writing reference.</p> <pre><code>from pulser_azure import AzureConnection\nfrom pulser.pulse import Pulse\nfrom pulser import QPUBackend, Sequence, Register\n\nconnection = AzureConnection()\n\n# Retrieve all QPU devices\ndevices = connection.fetch_available_devices()\ndevice = devices[\"pasqal.qpu.fresnel-can1\"]\n\n# Create a register of trapped atoms before performing operation on them\nregister = Register.square(5, 5).with_automatic_layout(device)\n\n# Declare the sequence of pulses to perform on the register\nsequence = Sequence(register, device)\nsequence.declare_channel(\"rydberg\", \"rydberg_global\")\nomega_max = sequence.declare_variable(\"omega_max\")\n\n# Add a pulse to that channel with the amplitude omega_max\ngeneric_pulse = Pulse.ConstantPulse(100, omega_max, 2, 0.0)\nsequence.add(generic_pulse, \"rydberg\")\n\n# Declare a backend based on the sequence and remote connection\nbackend = QPUBackend(sequence=sequence, connection=connection)\n\n# Run jobs with different arguments over the same sequence and register\nresults = backend.run(\n    job_params=[\n        {\"runs\": 5, \"variables\": {\"omega_max\": 12}},\n        {\"runs\": 10, \"variables\": {\"omega_max\": 6}},\n    ],\n    wait=True,\n)\nprint(results.results)\n</code></pre>"},{"location":"getting-started/#running-on-emulators","title":"Running on emulators","text":"<p>Pulser Azure exposes several remote emulator backends:</p> <ul> <li><code>RemoteEmuSVBackend</code> \u2014 state vector emulator</li> <li><code>RemoteEmuMPSBackend</code> \u2014 MPS emulator</li> <li><code>RemoteEmuFreeBackend</code> \u2014 free-tier emulator</li> </ul> <p>Use them in place of <code>QPUBackend</code> to run the same sequences without consuming real QPU time:</p> <pre><code>from pulser_azure import AzureConnection, RemoteEmuMPSBackend\nfrom pulser.pulse import Pulse\nfrom pulser import Sequence, Register\n\nconnection = AzureConnection()\n\n# Retrieve all QPU devices\ndevices = connection.fetch_available_devices()\ndevice = devices[\"pasqal.qpu.fresnel-can1\"]\n\n# Create a register of trapped atoms before performing operation on them\nregister = Register.square(5, 5).with_automatic_layout(device)\n\n# Declare the sequence of pulses to perform on the register\nsequence = Sequence(register, device)\nsequence.declare_channel(\"rydberg\", \"rydberg_global\")\nomega_max = sequence.declare_variable(\"omega_max\")\n\n# Add a pulse to that channel with the amplitude omega_max\ngeneric_pulse = Pulse.ConstantPulse(100, omega_max, 2, 0.0)\nsequence.add(generic_pulse, \"rydberg\")\n\n# Declare a backend based on the sequence and remote connection\nbackend = RemoteEmuMPSBackend(sequence=sequence, connection=connection)\n\n# Run jobs with different arguments over the same sequence and register\nresults = backend.run(\n    job_params=[\n        {\"runs\": 5, \"variables\": {\"omega_max\": 12}},\n        {\"runs\": 10, \"variables\": {\"omega_max\": 6}},\n    ],\n    wait=True,\n)\nprint(results.results)\n</code></pre>"},{"location":"reference/","title":"API reference","text":"<p>Top-level package \u2014 re-exports the public API.</p>"},{"location":"reference/#pulser_azure","title":"pulser_azure","text":""},{"location":"reference/#pulser_azure.AzureConnection","title":"AzureConnection","text":"<pre><code>AzureConnection(resource_id: str | None = None)\n</code></pre> <p>               Bases: <code>RemoteConnection</code></p> <p>Azure Quantum connection bridge.</p> <p>Parameters:</p> Name Type Description Default <code>resource_id</code> <code>str | None</code> <p>option resource ID of the Azure workspace, if the provided <code>resource_id</code> is <code>None</code>, the value is loaded from the <code>PULSER_AZURE_RESOURCE_ID</code> environment variable.</p> <code>None</code> Source code in <code>src/pulser_azure/connection.py</code> <pre><code>def __init__(self, resource_id: str | None = None):\n    credential = DefaultAzureCredential(\n        exclude_interactive_browser_credential=False\n    )\n    self._workspace = Workspace(\n        resource_id=resource_id or os.getenv(\"PULSER_AZURE_RESOURCE_ID\"),\n        credential=credential,\n    )\n    self._target_name_device_map: dict[str, Device] = {}\n    self._target_name_target_map: dict[str, Pasqal] = {}\n    self._device_name_target_map: dict[str, str] = {}\n</code></pre>"},{"location":"reference/#pulser_azure.AzureConnection.submit","title":"submit","text":"<pre><code>submit(\n    sequence: Sequence,\n    wait: bool = False,\n    open: bool = _UNSET,\n    batch_id: str | None = None,\n    emulation_config: EmulationConfig | None = None,\n    target_name: str | None = None,\n    **kwargs: Any,\n) -&gt; RemoteResults\n</code></pre> <p>Submit a job for execution.</p> Source code in <code>src/pulser_azure/connection.py</code> <pre><code>def submit(\n    self,\n    sequence: Sequence,\n    wait: bool = False,\n    open: bool = _UNSET,\n    batch_id: str | None = None,\n    emulation_config: EmulationConfig | None = None,\n    target_name: str | None = None,\n    **kwargs: Any,\n) -&gt; RemoteResults:\n    \"\"\"Submit a job for execution.\"\"\"\n    open_explicit = open is not _UNSET\n    open = True if open is _UNSET else open\n\n    if target_name is None:\n        target_name = self._device_name_target_map[sequence.device.name]\n\n    target = self._target_name_target_map.get(target_name)\n    if target is None:\n        raise RuntimeError(\n            f\"The target {target_name} isn't available on your workspace\"\n        )\n\n    job_params = make_json_compatible(kwargs.get(\"job_params\", []))\n\n    # Context manager use case (eg: with backend.open_batch()), even\n    # if connection had a session, create a new one as we're supposed to\n    # be in another context now.\n    if open_explicit is True and open:\n        batch_id = self._setup_session(target)\n\n        return RemoteResults(batch_id=batch_id, connection=self)\n\n    # Present when running backend.run() inside context manager\n    if batch_id:\n        job_ids = self._submit_jobs(\n            target=target,\n            sequence=sequence,\n            job_params=job_params,\n            emulation_config=emulation_config,\n        )\n    # Classic backend.run() call\n    else:\n        batch_id = self._setup_session(target)\n\n        job_ids = self._submit_jobs(\n            target=target,\n            sequence=sequence,\n            job_params=job_params,\n            emulation_config=emulation_config,\n        )\n\n    if wait:\n        for job_id in job_ids:\n            job = self._workspace.get_job(job_id)\n            job.wait_until_completed()\n\n    if not open:\n        self._close_batch(batch_id)\n\n    return RemoteResults(batch_id=batch_id, connection=self, job_ids=job_ids)\n</code></pre>"},{"location":"reference/#pulser_azure.AzureConnection.fetch_available_devices","title":"fetch_available_devices","text":"<pre><code>fetch_available_devices() -&gt; dict[str, Device]\n</code></pre> <p>Fetches the devices available through this connection.</p> Source code in <code>src/pulser_azure/connection.py</code> <pre><code>def fetch_available_devices(self) -&gt; dict[str, Device]:\n    \"\"\"Fetches the devices available through this connection.\"\"\"\n\n    if not self._target_name_device_map:\n        # Only retrieve targets available through current workspace provider's plan\n        raw_targets = self._workspace.get_targets(provider_id=_PASQAL_PROVIDER_ID)\n        if isinstance(raw_targets, Target):\n            targets: list[Target] = [raw_targets]\n        else:\n            targets = list(cast(Iterable[Target], raw_targets))\n\n        # Only build real QPU devices\n        devices = [\n            Device.from_abstract_repr(spec[\"specs\"])\n            for spec in self._get_device_specs()\n        ]\n\n        # Iterate over all PasqalTarget values rather than only workspace\n        # targets: free-plan users need real Device objects to author and\n        # validate sequences locally even when their workspace only exposes\n        # an emulator target (e.g. SIM_EMU_FREE).\n        for _target in _TARGETS:\n            target = next(\n                (t for t in targets if t.name == _target.enum_value), None\n            )\n\n            device = next(\n                (\n                    d\n                    for d in devices\n                    if d.name == _target.enum_name.removeprefix(\"QPU_\")\n                ),\n                None,\n            )\n\n            if device:\n                self._target_name_device_map[_target.enum_value] = device\n                self._device_name_target_map[device.name] = _target.enum_value\n\n            if target:\n                # Targets returned from the pasqal provider are Pasqal\n                # instances at runtime; cast for the type checker.\n                self._target_name_target_map[_target.enum_value] = cast(\n                    Pasqal, target\n                )\n\n    return {k: v for k, v in self._target_name_device_map.items()}\n</code></pre>"},{"location":"reference/#pulser_azure.AzureConnection.supports_open_batch","title":"supports_open_batch","text":"<pre><code>supports_open_batch() -&gt; bool\n</code></pre> <p>Flag to confirm this class can support creating an open batch.</p> Source code in <code>src/pulser_azure/connection.py</code> <pre><code>def supports_open_batch(self) -&gt; bool:\n    \"\"\"Flag to confirm this class can support creating an open batch.\"\"\"\n    return True\n</code></pre>"},{"location":"reference/#pulser_azure.RemoteEmuSVBackend","title":"RemoteEmuSVBackend","text":"<pre><code>RemoteEmuSVBackend(\n    sequence: Sequence,\n    connection: RemoteConnection,\n    mimic_qpu: bool = False,\n    *,\n    config: EmulationConfig | None = None,\n)\n</code></pre> <p>               Bases: <code>BaseRemoteEmulatorBackend</code></p> Source code in <code>src/pulser_azure/backends.py</code> <pre><code>def __init__(\n    self,\n    sequence: Sequence,\n    connection: RemoteConnection,\n    mimic_qpu: bool = False,\n    *,\n    config: EmulationConfig | None = None,\n) -&gt; None:\n    super().__init__(\n        sequence,\n        connection,\n        mimic_qpu,\n        config=config if config is not None else self._default_config,\n    )\n</code></pre>"},{"location":"reference/#pulser_azure.RemoteEmuSVBackend.target_name","title":"target_name  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>target_name = 'pasqal.sim.emu-sv'\n</code></pre>"},{"location":"reference/#pulser_azure.RemoteEmuMPSBackend","title":"RemoteEmuMPSBackend","text":"<pre><code>RemoteEmuMPSBackend(\n    sequence: Sequence,\n    connection: RemoteConnection,\n    mimic_qpu: bool = False,\n    *,\n    config: EmulationConfig | None = None,\n)\n</code></pre> <p>               Bases: <code>BaseRemoteEmulatorBackend</code></p> Source code in <code>src/pulser_azure/backends.py</code> <pre><code>def __init__(\n    self,\n    sequence: Sequence,\n    connection: RemoteConnection,\n    mimic_qpu: bool = False,\n    *,\n    config: EmulationConfig | None = None,\n) -&gt; None:\n    super().__init__(\n        sequence,\n        connection,\n        mimic_qpu,\n        config=config if config is not None else self._default_config,\n    )\n</code></pre>"},{"location":"reference/#pulser_azure.RemoteEmuMPSBackend.target_name","title":"target_name  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>target_name = 'pasqal.sim.emu-mps'\n</code></pre>"},{"location":"reference/#pulser_azure.RemoteEmuFreeBackend","title":"RemoteEmuFreeBackend","text":"<pre><code>RemoteEmuFreeBackend(\n    sequence: Sequence,\n    connection: RemoteConnection,\n    mimic_qpu: bool = False,\n    *,\n    config: EmulationConfig | None = None,\n)\n</code></pre> <p>               Bases: <code>BaseRemoteEmulatorBackend</code></p> Source code in <code>src/pulser_azure/backends.py</code> <pre><code>def __init__(\n    self,\n    sequence: Sequence,\n    connection: RemoteConnection,\n    mimic_qpu: bool = False,\n    *,\n    config: EmulationConfig | None = None,\n) -&gt; None:\n    super().__init__(\n        sequence,\n        connection,\n        mimic_qpu,\n        config=config if config is not None else self._default_config,\n    )\n</code></pre>"},{"location":"reference/#pulser_azure.RemoteEmuFreeBackend.target_name","title":"target_name  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>target_name = 'pasqal.sim.emu-free'\n</code></pre>"},{"location":"reference/backends/","title":"<code>pulser_azure.backends</code>","text":""},{"location":"reference/backends/#pulser_azure.backends","title":"pulser_azure.backends","text":""},{"location":"reference/backends/#pulser_azure.backends.BaseRemoteEmulatorBackend","title":"BaseRemoteEmulatorBackend","text":"<pre><code>BaseRemoteEmulatorBackend(\n    sequence: Sequence,\n    connection: RemoteConnection,\n    mimic_qpu: bool = False,\n    *,\n    config: EmulationConfig | None = None,\n)\n</code></pre> <p>               Bases: <code>RemoteBackend</code></p> <p>Base class for Pasqal cloud emulator backends exposed via Azure.</p> Subclasses must set <ul> <li><code>target_name</code>:     the <code>PasqalTarget</code> the backend should route to.</li> <li><code>_default_config</code>: the <code>EmulationConfig</code> used when the caller does                      not explicitly pass <code>config=...</code>.</li> </ul> Source code in <code>src/pulser_azure/backends.py</code> <pre><code>def __init__(\n    self,\n    sequence: Sequence,\n    connection: RemoteConnection,\n    mimic_qpu: bool = False,\n    *,\n    config: EmulationConfig | None = None,\n) -&gt; None:\n    super().__init__(\n        sequence,\n        connection,\n        mimic_qpu,\n        config=config if config is not None else self._default_config,\n    )\n</code></pre>"},{"location":"reference/backends/#pulser_azure.backends.BaseRemoteEmulatorBackend.target_name","title":"target_name  <code>instance-attribute</code>","text":"<pre><code>target_name: PasqalTarget\n</code></pre>"},{"location":"reference/backends/#pulser_azure.backends.RemoteEmuSVBackend","title":"RemoteEmuSVBackend","text":"<pre><code>RemoteEmuSVBackend(\n    sequence: Sequence,\n    connection: RemoteConnection,\n    mimic_qpu: bool = False,\n    *,\n    config: EmulationConfig | None = None,\n)\n</code></pre> <p>               Bases: <code>BaseRemoteEmulatorBackend</code></p> Source code in <code>src/pulser_azure/backends.py</code> <pre><code>def __init__(\n    self,\n    sequence: Sequence,\n    connection: RemoteConnection,\n    mimic_qpu: bool = False,\n    *,\n    config: EmulationConfig | None = None,\n) -&gt; None:\n    super().__init__(\n        sequence,\n        connection,\n        mimic_qpu,\n        config=config if config is not None else self._default_config,\n    )\n</code></pre>"},{"location":"reference/backends/#pulser_azure.backends.RemoteEmuSVBackend.target_name","title":"target_name  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>target_name = 'pasqal.sim.emu-sv'\n</code></pre>"},{"location":"reference/backends/#pulser_azure.backends.RemoteEmuMPSBackend","title":"RemoteEmuMPSBackend","text":"<pre><code>RemoteEmuMPSBackend(\n    sequence: Sequence,\n    connection: RemoteConnection,\n    mimic_qpu: bool = False,\n    *,\n    config: EmulationConfig | None = None,\n)\n</code></pre> <p>               Bases: <code>BaseRemoteEmulatorBackend</code></p> Source code in <code>src/pulser_azure/backends.py</code> <pre><code>def __init__(\n    self,\n    sequence: Sequence,\n    connection: RemoteConnection,\n    mimic_qpu: bool = False,\n    *,\n    config: EmulationConfig | None = None,\n) -&gt; None:\n    super().__init__(\n        sequence,\n        connection,\n        mimic_qpu,\n        config=config if config is not None else self._default_config,\n    )\n</code></pre>"},{"location":"reference/backends/#pulser_azure.backends.RemoteEmuMPSBackend.target_name","title":"target_name  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>target_name = 'pasqal.sim.emu-mps'\n</code></pre>"},{"location":"reference/backends/#pulser_azure.backends.RemoteEmuFreeBackend","title":"RemoteEmuFreeBackend","text":"<pre><code>RemoteEmuFreeBackend(\n    sequence: Sequence,\n    connection: RemoteConnection,\n    mimic_qpu: bool = False,\n    *,\n    config: EmulationConfig | None = None,\n)\n</code></pre> <p>               Bases: <code>BaseRemoteEmulatorBackend</code></p> Source code in <code>src/pulser_azure/backends.py</code> <pre><code>def __init__(\n    self,\n    sequence: Sequence,\n    connection: RemoteConnection,\n    mimic_qpu: bool = False,\n    *,\n    config: EmulationConfig | None = None,\n) -&gt; None:\n    super().__init__(\n        sequence,\n        connection,\n        mimic_qpu,\n        config=config if config is not None else self._default_config,\n    )\n</code></pre>"},{"location":"reference/backends/#pulser_azure.backends.RemoteEmuFreeBackend.target_name","title":"target_name  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>target_name = 'pasqal.sim.emu-free'\n</code></pre>"},{"location":"reference/connection/","title":"<code>pulser_azure.connection</code>","text":""},{"location":"reference/connection/#pulser_azure.connection","title":"pulser_azure.connection","text":""},{"location":"reference/connection/#pulser_azure.connection.AzureConnection","title":"AzureConnection","text":"<pre><code>AzureConnection(resource_id: str | None = None)\n</code></pre> <p>               Bases: <code>RemoteConnection</code></p> <p>Azure Quantum connection bridge.</p> <p>Parameters:</p> Name Type Description Default <code>resource_id</code> <code>str | None</code> <p>option resource ID of the Azure workspace, if the provided <code>resource_id</code> is <code>None</code>, the value is loaded from the <code>PULSER_AZURE_RESOURCE_ID</code> environment variable.</p> <code>None</code> Source code in <code>src/pulser_azure/connection.py</code> <pre><code>def __init__(self, resource_id: str | None = None):\n    credential = DefaultAzureCredential(\n        exclude_interactive_browser_credential=False\n    )\n    self._workspace = Workspace(\n        resource_id=resource_id or os.getenv(\"PULSER_AZURE_RESOURCE_ID\"),\n        credential=credential,\n    )\n    self._target_name_device_map: dict[str, Device] = {}\n    self._target_name_target_map: dict[str, Pasqal] = {}\n    self._device_name_target_map: dict[str, str] = {}\n</code></pre>"},{"location":"reference/connection/#pulser_azure.connection.AzureConnection.submit","title":"submit","text":"<pre><code>submit(\n    sequence: Sequence,\n    wait: bool = False,\n    open: bool = _UNSET,\n    batch_id: str | None = None,\n    emulation_config: EmulationConfig | None = None,\n    target_name: str | None = None,\n    **kwargs: Any,\n) -&gt; RemoteResults\n</code></pre> <p>Submit a job for execution.</p> Source code in <code>src/pulser_azure/connection.py</code> <pre><code>def submit(\n    self,\n    sequence: Sequence,\n    wait: bool = False,\n    open: bool = _UNSET,\n    batch_id: str | None = None,\n    emulation_config: EmulationConfig | None = None,\n    target_name: str | None = None,\n    **kwargs: Any,\n) -&gt; RemoteResults:\n    \"\"\"Submit a job for execution.\"\"\"\n    open_explicit = open is not _UNSET\n    open = True if open is _UNSET else open\n\n    if target_name is None:\n        target_name = self._device_name_target_map[sequence.device.name]\n\n    target = self._target_name_target_map.get(target_name)\n    if target is None:\n        raise RuntimeError(\n            f\"The target {target_name} isn't available on your workspace\"\n        )\n\n    job_params = make_json_compatible(kwargs.get(\"job_params\", []))\n\n    # Context manager use case (eg: with backend.open_batch()), even\n    # if connection had a session, create a new one as we're supposed to\n    # be in another context now.\n    if open_explicit is True and open:\n        batch_id = self._setup_session(target)\n\n        return RemoteResults(batch_id=batch_id, connection=self)\n\n    # Present when running backend.run() inside context manager\n    if batch_id:\n        job_ids = self._submit_jobs(\n            target=target,\n            sequence=sequence,\n            job_params=job_params,\n            emulation_config=emulation_config,\n        )\n    # Classic backend.run() call\n    else:\n        batch_id = self._setup_session(target)\n\n        job_ids = self._submit_jobs(\n            target=target,\n            sequence=sequence,\n            job_params=job_params,\n            emulation_config=emulation_config,\n        )\n\n    if wait:\n        for job_id in job_ids:\n            job = self._workspace.get_job(job_id)\n            job.wait_until_completed()\n\n    if not open:\n        self._close_batch(batch_id)\n\n    return RemoteResults(batch_id=batch_id, connection=self, job_ids=job_ids)\n</code></pre>"},{"location":"reference/connection/#pulser_azure.connection.AzureConnection.fetch_available_devices","title":"fetch_available_devices","text":"<pre><code>fetch_available_devices() -&gt; dict[str, Device]\n</code></pre> <p>Fetches the devices available through this connection.</p> Source code in <code>src/pulser_azure/connection.py</code> <pre><code>def fetch_available_devices(self) -&gt; dict[str, Device]:\n    \"\"\"Fetches the devices available through this connection.\"\"\"\n\n    if not self._target_name_device_map:\n        # Only retrieve targets available through current workspace provider's plan\n        raw_targets = self._workspace.get_targets(provider_id=_PASQAL_PROVIDER_ID)\n        if isinstance(raw_targets, Target):\n            targets: list[Target] = [raw_targets]\n        else:\n            targets = list(cast(Iterable[Target], raw_targets))\n\n        # Only build real QPU devices\n        devices = [\n            Device.from_abstract_repr(spec[\"specs\"])\n            for spec in self._get_device_specs()\n        ]\n\n        # Iterate over all PasqalTarget values rather than only workspace\n        # targets: free-plan users need real Device objects to author and\n        # validate sequences locally even when their workspace only exposes\n        # an emulator target (e.g. SIM_EMU_FREE).\n        for _target in _TARGETS:\n            target = next(\n                (t for t in targets if t.name == _target.enum_value), None\n            )\n\n            device = next(\n                (\n                    d\n                    for d in devices\n                    if d.name == _target.enum_name.removeprefix(\"QPU_\")\n                ),\n                None,\n            )\n\n            if device:\n                self._target_name_device_map[_target.enum_value] = device\n                self._device_name_target_map[device.name] = _target.enum_value\n\n            if target:\n                # Targets returned from the pasqal provider are Pasqal\n                # instances at runtime; cast for the type checker.\n                self._target_name_target_map[_target.enum_value] = cast(\n                    Pasqal, target\n                )\n\n    return {k: v for k, v in self._target_name_device_map.items()}\n</code></pre>"},{"location":"reference/connection/#pulser_azure.connection.AzureConnection.supports_open_batch","title":"supports_open_batch","text":"<pre><code>supports_open_batch() -&gt; bool\n</code></pre> <p>Flag to confirm this class can support creating an open batch.</p> Source code in <code>src/pulser_azure/connection.py</code> <pre><code>def supports_open_batch(self) -&gt; bool:\n    \"\"\"Flag to confirm this class can support creating an open batch.\"\"\"\n    return True\n</code></pre>"}]}